{
  "name": "webkuhmanis",
  "version": "1.0.0",
  "description": "Payment OTP Confirmation System with Animations",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm start -w backend",
    "setup": "npm install && npm run migrate -w backend"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/howler": "^2.2.12",
    "concurrently": "^8.2.2"
  }
}
